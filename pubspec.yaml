name: result
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  freezed: ^0.12.6
  freezed_annotation: ^0.12.0
  rxdart: ^0.25.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  dart_lint:
    path: /Users/futoshi/project/dart_lint

flutter:

